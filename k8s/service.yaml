apiVersion: v1
kind: Service
metadata:
  name: temperature-proxy-api
  labels:
    app: temperature-proxy-api
spec:
  selector:
    app: temperature-proxy-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
